# Copy this file to .env.local and adjust as needed.

# "true" (default) = use built-in mock data/generator, no backend needed.
# "false" = connect to the real Sentinel gateway WebSocket below.
VITE_USE_MOCK=true

# Only used when VITE_USE_MOCK=false
VITE_WS_URL=ws://localhost:8000/ws
